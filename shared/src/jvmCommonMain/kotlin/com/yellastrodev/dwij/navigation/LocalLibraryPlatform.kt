package com.yellastrodev.dwij.navigation

/** Системные действия shared-route локальной медиатеки. */
interface LocalLibraryPlatform {
    fun startLocalLibrarySync()

    fun showTrackHideFailed()
}
