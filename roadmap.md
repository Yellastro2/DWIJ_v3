# Roadmap

- Реализовать межустройственную синхронизацию плеера через Ynison: публиковать очередь и состояние `Движа`, принимать удалённые команды.
  Обеспечить стабильный `deviceId`, переподключение с восстановлением состояния и совместимость с `/play-audio` и rotor feedback.

- все треки локальные очень долго и каждый раз снова грузятся — реализовано 2026-08-03: экран читает готовый индекс Room, MediaStore сканируется отдельно и применяется дельтой
- не обновляется метадата у локальных треков если её обновлять на стороне — реализовано 2026-08-03 для изменений, опубликованных MediaStore: изменённая запись обновляет source-трек и Song

- если трек недоступен в ЯМ но есть в кеше, лонгтап по треку показывает контекст меню в котором кнопка "выгрузить" корорая в папку загрузки устройства выгружает трек,
  надо там поправить метаданные на всякий случай еще в файле.
- где то на главной сделать индикатор что есть неразрешенные SongMatchCandidateEntity и по нажатию открывать список их чтоб юзер сидел разрешал — реализовано 2026-08-03
