package com.yellastrodev.dwij.data.source

import com.yellastrodev.dwij.entities.dYaPlaylist
import com.yellastrodev.yandexmusiclib.entities.YaPlaylist

//class PlaylistLocalSource(private val dao: dPlaylistDao) {
//    suspend fun get(id: String): YaPlaylist? = dao.getdPlaylistById(id)
//    suspend fun getAll(): List<YaPlaylist> = dao.getAlldPlaylists()
//    suspend fun save(dPlaylist: YaPlaylist) = dao.insert(dPlaylist)
//    suspend fun saveAll(dPlaylists: List<YaPlaylist>) = dao.insertAll(dPlaylists)
//}